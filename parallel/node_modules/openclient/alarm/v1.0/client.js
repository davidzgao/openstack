var base = require("../../client/base");

var AlarmManager = require("./alarm");


var Alarm = base.Client.extend({
  service_type: "alarm",
  version: "1.0",

  init: function (options) {
    this._super(options);

    this.alarms = new AlarmManager(this);
  }
});

module.exports = Alarm;
