var base = require("../../client/base")

var AccountManager = base.Manager.extend({
  namespace: "accounts",
  prepare_data: function (data) {
    if (this.use_raw_data) return data;
    var wrapped_data = {};
    wrapped_data = data;
    return wrapped_data;
  },
})

module.exports = AccountManager;
