var base = require("../../client/base"),
    PriceManager = require('./prices');

var Funnel = base.Client.extend({
  service_type: "price",
  version: "1.0",

  init: function (options) {
    this._super(options);

    this.prices = new PriceManager(this);
  }
});

module.exports = Funnel;
