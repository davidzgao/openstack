var base = require("../../client/base"),
    error = require("../../client/error");

var FloatingIPManager = base.Manager.extend({
  namespace: "floatingips",    
  plural: "floatingips"
});

module.exports = FloatingIPManager;
