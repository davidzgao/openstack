var base = require("../../client/base"),
    error = require("../../client/error");

var SecurityGroupsManager = base.Manager.extend({
  namespace: "security-groups",
  plural: "security_groups"
});

module.exports = SecurityGroupsManager;
