var base = require("../../client/base");

var RegionManager = base.Manager.extend({
  namespace: "regions",
  plural: "regions",


  init: function(client) {
    this._super(client);
    this.method_map.update = "patch";
  }
});

module.exports = RegionManager;
