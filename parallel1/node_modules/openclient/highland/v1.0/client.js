var base = require("../../client/base");

var Rules = require("./rules");

var Highland = base.Client.extend({
  service_type: "maintenance",
  version: "1.0",

  init: function (options) {
    this._super(options);

    this.rules = new Rules(this);
  }
});

module.exports = Highland;
